# Pinned to versions known to work together as of the 2026.07 environment.
# Loosen any of these if your team's environment requires it — nothing here
# is exotic, but mixing torch 1.x and 2.x ultralytics can be painful.

# Core ML / detection
ultralytics>=8.2.0,<9.0
torch>=2.1.0
torchvision>=0.16.0

# Data + analysis
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
Pillow>=10.0

# Notebook environment (optional — only needed if you open
# notebooks/dataset_analysis.ipynb locally)
jupyter>=1.0
ipykernel>=6.0
nbformat>=5.10

# CLI niceties
tqdm>=4.65
pyyaml>=6.0
